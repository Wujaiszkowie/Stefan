Klonowanie repo:
```
git clone https://github.com/username/repo.git
Username: <Twój username>
Password: <Token zamiast hasła>
```

zapis tokenu:
```
git config --global credential.helper store
```

odpalenie frontendu, w głównym katalogu:
```
docker compose up --build
```